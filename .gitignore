# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.worktrees

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Auto Claude data directory
.auto-claude/

# Git worktrees
.worktrees/

# Environment variables (keep .env.example)
.env
.env.local
.env.production.local

# Sorties de Lighthouse CI — rapports de mesure, jamais des sources.
# `upload.target: filesystem` (voir .lighthouserc.json) les écrit ici.
.lighthouseci/
lighthouse-rapports/

lighthouse-report.json

# ⛔ AJOUTÉ LE 19/09/2026 APRÈS UNE ERREUR RÉELLE.
# 21 images (10,7 Mo) référencées par AUCUNE page, jamais versionnées jusqu'ici —
# par discipline manuelle. Un git add public/ trop large les a emportées dans le
# commit d167a30 et poussées. La discipline ne suffisait pas : la règle est ici.
# ⚠️ Les fichiers restent sur le disque, ils ne partent simplement jamais.
/public/world/
/audit-auto/
