# ============================================================================
# GL DIGITAL LAB - Pipeline CI/CD Complet
# ============================================================================
# 🔍 Lint & Format check
# 🧪 Tests (si présents)
# 📦 Build production
# 📊 Lighthouse audit
# 🚀 Deploy FTP
# 💬 Notification Discord (optionnel)
# ============================================================================

name: 🚀 CI/CD Pipeline

on:
  # ⛔ LE DÉCLENCHEMENT AUTOMATIQUE SUR PUSH A ÉTÉ RETIRÉ LE 26/09/2026, ET C'EST VOULU.
  # Le site cinématique occupe désormais la RACINE de gldigitallab.fr. Si ce pipeline se
  # relançait à chaque push sur main, il réécrirait `index.html` à la racine et reprendrait
  # l'accueil — sans erreur, sans trace, et personne ne s'en apercevrait avant qu'un
  # visiteur ne le signale.
  # ⇒ Il reste disponible À LA MAIN : onglet Actions → « CI/CD Pipeline » → Run workflow.
  #   Rien n'est supprimé, rien n'est perdu, il suffit de le demander.
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  # Node 22 et non 20 : le prérendu (scripts/prerendre.js) pilote Chrome par le
  # protocole de débogage et a besoin du WebSocket global, absent de Node 20.
  # Le build ÉCHOUE si une URL du sitemap rend une page 404 — c'est voulu :
  # le déploiement ne doit pas partir avec un plan de site faux.
  NODE_VERSION: '24'

jobs:
  # ═══════════════════════════════════════════════════════════════════════════
  # JOB 1: Lint & Quality Check
  # ═══════════════════════════════════════════════════════════════════════════
  lint:
    name: 🔍 Lint & Quality
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v7

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🔍 Lint check  (NE FAIT RIEN — voir le commentaire)
        # ⚠️ CETTE ÉTAPE NE VÉRIFIE RIEN, ET ELLE DOIT LE DIRE.
        # Constaté le 13/09/2026 : `package.json` ne déclare **aucun script `lint`**,
        # il n'existe **aucune configuration ESLint** dans le dépôt, et **aucune
        # dépendance ESLint** n'est installée. `npm run lint --if-present` sort donc
        # en 0 sans rien exécuter, et `continue-on-error` couvre le reste.
        #
        # Deux lectures, et la seconde est la bonne :
        #   · c'est un échafaudage honnête — `--if-present` est la façon standard
        #     d'écrire une CI qui tolère un script absent ;
        #   · MAIS LE NOM MENT. Un job « Lint & Quality » avec une étape « Lint check »
        #     laisse croire que le code est vérifié. **Un verrou qui ne peut pas
        #     échouer n'est pas un verrou, c'est un tampon** — la formule est du
        #     studio, et elle s'applique ici.
        #
        # On garde l'échafaudage (il servira le jour où un linter arrive) et on
        # supprime la fausse impression : même traitement que « Check file sizes ».
        run: |
          echo "⚠️  AUCUN LINTER N'EST CONFIGURÉ DANS CE DÉPÔT."
          echo "    'npm run lint --if-present' ne vérifie RIEN : le script est absent,"
          echo "    il n'y a ni configuration ESLint ni dépendance ESLint installée."
          echo "    Cette étape ne peut pas échouer. Elle est conservée pour le jour où"
          echo "    un linter sera ajouté — d'ici là, elle ne doit pas être prise pour un verrou."
          npm run lint --if-present || true

      - name: 📏 Check file sizes  (INFORMATIF — ce n'est pas un verrou)
        # ⚠ CETTE ÉTAPE N'EST PAS UN VERROU, ET ELLE NE DOIT PAS ÊTRE PRISE POUR UN.
        # `find … -exec ls -lh` AFFICHE, et ne sort jamais en erreur : quoi qu'il
        # trouve, le job continue. Elle regarde de plus `src/`, dont la taille ne
        # dit rien de ce que le visiteur télécharge — Vite hache, découpe et
        # compresse ensuite.
        #
        # Le seul verrou chiffré de la charte qui porte sur le poids, « < 1 MB par
        # page », n'avait donc aucun juge : `verifier-verrous.mjs` renvoyait la
        # question ici, et ici personne ne jugeait. Constaté le 13/09/2026.
        # Il est désormais tenu par `scripts/verifier-poids.mjs`, dans le job
        # `build`, sur `dist/` — là où le chiffre existe vraiment.
        run: |
          echo "📊 Checking large files..."
          find src -name "*.vue" -size +100k -exec ls -lh {} \;
          find src -name "*.js" -size +100k -exec ls -lh {} \;

      - name: 🔒 Verrous de la signature qualité
        # Les verrous (AA · reduced-motion · curseur natif · mode sobre · garde-fou
        # juridique) ne vivaient que dans des COMMENTAIRES. Ce dépôt en a déjà payé
        # le prix : `cursor: none` a survécu au retrait du curseur personnalisé —
        # masquer le curseur système sans remplaçant laisse le visiteur SANS curseur.
        # Un verrou décrit dans un commentaire n'est pas un verrou : c'est une
        # intention. Cette étape le transforme en mesure, et échoue s'il casse.
        #
        # Elle lit les jetons RÉELLEMENT définis dans variables.css, donc elle suit
        # automatiquement tout changement de palette (D1 clair, D4/D5 sombre, ...).
        # Constaté le 11/09/2026 : elle a signalé --alert à 4,45:1 (seuil 4,5) et
        # --rule-strong à 1,96:1 (seuil 3) — deux défauts réels, corrigés depuis.
        run: node scripts/verifier-verrous.mjs

  # ═══════════════════════════════════════════════════════════════════════════
  # JOB 2: Build & Test
  # ═══════════════════════════════════════════════════════════════════════════
  build:
    name: 📦 Build & Test
    runs-on: ubuntu-latest
    needs: lint
    
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v7

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🧪 Run tests  (NE FAIT RIEN — voir le commentaire)
        # ⚠️ Même constat que pour le lint, mesuré le 13/09/2026 : `package.json` ne
        # déclare **aucun script `test`**, et **aucun cadre de test** (vitest, jest…)
        # n'est installé. L'étape sort en 0 sans rien exécuter.
        #
        # Le dire ici plutôt que de laisser croire que le projet est testé.
        run: |
          echo "⚠️  AUCUN TEST N'EST CONFIGURÉ DANS CE DÉPÔT."
          echo "    'npm test --if-present' n'exécute RIEN : le script est absent et"
          echo "    aucun cadre de test n'est installé."
          npm test --if-present || true

      - name: 🌐 Chrome pour le prérendu
        # ⛔ POURQUOI CETTE ÉTAPE EXISTE — DÉFAUT OUVERT, 22/09/2026.
        #
        # Mesure : le passage CI n°135 (commit `ffb8c09`) a échoué sur
        # `🔨 Build production` en 79 s, et `🚀 Deploy Production` a été SAUTÉ.
        # Le passage n°134 (`c3c9cc6`, dix minutes plus tôt) avait réussi.
        # Le même build sur un clone frais, en local, sort en 0 : la cause est
        # donc propre à l'environnement du coureur.
        #
        # Le seul chemin du build qui tue le processus sans que ce soit
        # reproductible en local est dans `scripts/prerendre.js` :
        #     const chrome = trouverChrome()
        #     if (!chrome) { … process.exit(1) }
        # Or `trouverChrome()` cherche des chemins FIXES (`/usr/bin/google-chrome`,
        # `/opt/google/chrome/chrome`…), et `ubuntu-latest` est une image MOUVANTE :
        # ce qu'elle embarque change sans que ce dépôt change.
        # ⚠️ LA CAUSE N'EST PAS PROUVÉE : GitHub ne livre pas les journaux sans
        # authentification (HTTP 403). Elle est FORTEMENT SUSPECTÉE, pas établie.
        #
        # ⛔ ET ON NE SE CONTENTE PAS DE LA CONTOURNER. Si Chrome manque, on
        # l'INSTALLE, on ne saute pas le prérendu : sans lui, le HTML livré n'a
        # NI navigation NI pied de page — c'est précisément le défaut que le
        # studio a mis des semaines à fermer, et que `verifier-topographie.mjs`
        # mesure sur 28/28 routes.
        run: |
          set -euo pipefail
          CHROME=""
          for c in "${CHROME_PATH:-}" /usr/bin/google-chrome /usr/bin/google-chrome-stable \
                   /opt/google/chrome/chrome /usr/bin/chromium /usr/bin/chromium-browser; do
            if [ -n "$c" ] && [ -x "$c" ]; then CHROME="$c"; break; fi
          done
          if [ -z "$CHROME" ]; then
            echo "Chrome absent de l'image — installation depuis le dépôt Google."
            sudo apt-get update -qq
            sudo apt-get install -y -qq google-chrome-stable || true
            for c in /usr/bin/google-chrome /usr/bin/google-chrome-stable /opt/google/chrome/chrome; do
              if [ -x "$c" ]; then CHROME="$c"; break; fi
            done
          fi
          if [ -z "$CHROME" ]; then
            echo "::error::Chrome INTROUVABLE sur le coureur. Le prérendu ne peut pas tourner, et sans lui le HTML livré n'a ni navigation ni pied de page."
            exit 1
          fi
          echo "Chrome trouvé : $CHROME"
          "$CHROME" --version
          echo "CHROME_PATH=$CHROME" >> "$GITHUB_ENV"

      - name: 🈯 La porte des langues — elle REFUSE une traduction incomplète
        # ⭐ AJOUTÉE LE 23/09/2026, ET C'EST LA DEMANDE DU CHANTIER : « la porte de build
        # qui REFUSE une traduction incomplète ».
        #
        # ⛔ POURQUOI ELLE MANQUAIT, ET CE QUI A CHANGÉ. `package.json` portait ce constat,
        # mot pour mot : « LA PORTE DE LANGUES N'EST PAS DANS LE BUILD » — parce qu'elle
        # n'avait jamais été éprouvée dans la chaîne, et parce qu'un cas l'aurait bloquée
        # à tort : quand `langues.js` ne se charge pas, elle sortait en **2**, EN DEHORS
        # du mode `--porte`. *Un `langues.js` illisible aurait donc arrêté la construction
        # du site pour une raison qui n'est PAS un défaut de traduction.*
        #
        # ⭐ CE QUI A ÉTÉ FAIT AVANT DE LA BRANCHER, ET DANS CET ORDRE :
        #   ① le cas « je ne peux pas lire la liste des langues » **ne bloque plus** en mode
        #      `--porte` — et il le DIT à voix haute (`scripts/verifier-langues.mjs`) ;
        #   ② la porte a été **éprouvée sur quatre cas** : langues.js illisible avec et sans
        #      `--porte`, `de.js` incomplet, `de.js` complet. Résultat mesuré : **0 · 2 · 1 · 0**.
        #      *Elle sait donc dire NON sur un vrai défaut, et OUI quand tout va bien.*
        #
        # ⚠️ ET CE QU'ELLE REFUSE, EN CLAIR : une clé **manquante**, **vide**, ou **en trop**
        # dans une langue active. *Une clé absente qui retombe sur le français donne une page
        # qui a l'air terminée — elle ment à la relecture, c'est-à-dire trop tard.*
        # ⚠️ Une langue **non commencée** ne bloque PAS : *c'est un chantier, et une langue
        # qui n'existe pas ne peut pas être en retard.*
        run: |
          set -euo pipefail
          echo "::group::0/8 langues — la porte i18n"
          node scripts/verifier-langues.mjs --porte
          echo "::endgroup::"

      - name: 🔨 Build production
        # ⛔ LE BUILD EST DÉCOUPÉ EN ÉTAPES NOMMÉES, ET CE N'EST PAS COSMÉTIQUE.
        # Mesure du 22/09/2026 : `npm run build` a échoué en CI, et GitHub n'a pu
        # nommer que l'étape qui l'englobe — pas la commande coupable. On a perdu
        # du temps à DEVINER. `::group::` fait que le journal nomme toujours la
        # phase, et qu'une panne se lit au lieu de se déduire.
        # ⚠️ La chaîne reproduite ici est celle de `package.json`, lue et non
        # supposée : prebuild → sitemap && ecole && arche && vite build &&
        # prerendre → postbuild.
        run: |
          set -euo pipefail
          echo "::group::1/7 prebuild — generer-etat.mjs"
          node scripts/generer-etat.mjs
          echo "::endgroup::"

          echo "::group::2/7 sitemap — generer-sitemap.mjs"
          node scripts/generer-sitemap.mjs
          echo "::endgroup::"

          echo "::group::3/7 ecole — publier-ecole.mjs"
          node scripts/publier-ecole.mjs
          echo "::endgroup::"

          echo "::group::4/7 arche — generer-arche.mjs"
          node scripts/generer-arche.mjs
          echo "::endgroup::"

          echo "::group::5/7 vite build"
          npx vite build
          echo "::endgroup::"

          echo "::group::6/7 prerendre — prerendre-boucle.mjs (passes reprises)"
          node scripts/prerendre-boucle.mjs
          echo "::endgroup::"

          echo "::group::7/7 postbuild — generer-etat.mjs + relever-dans-html.mjs"
          node scripts/generer-etat.mjs
          node scripts/relever-dans-html.mjs
          echo "::endgroup::"


          # ⭐⭐⭐ 8/8 — LA VITRINE PREND LA RACINE. DERNIÈRE ÉCRITURE, ET ELLE DOIT L'ÊTRE.
          # ⛔ MESURE DU 24/09/2026 : ce script était accroché à `npm run build` dans
          #    `package.json` — et il ne tournait JAMAIS en CI. **Parce que la CI
          #    n'exécute pas `npm run build`** : elle déroule ses propres étapes 1/7 à
          #    7/7, une par une. *Un script branché sur une commande que personne ne
          #    lance n'est pas un script : c'est un vœu.*
          # ⚠️ ET IL PASSE EN DERNIER, APRÈS le postbuild : `prerendre` écrit
          #    `dist/index.html` (la route `/`), et `relever-dans-html.mjs` y touche
          #    ensuite. Poser la vitrine avant ces deux-là serait l'écraser en silence
          #    — c'est exactement ce qui s'est passé au premier essai.
          echo "::group::8/8 la vitrine prend la racine"
          node scripts/poser-vitrine.mjs
          echo "::endgroup::"
        env:
          NODE_ENV: production

      - name: 📊 Build stats
        run: |
          echo "## 📦 Build Output" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| File | Size |" >> $GITHUB_STEP_SUMMARY
          echo "|------|------|" >> $GITHUB_STEP_SUMMARY
          find dist -name "*.js" -exec ls -lh {} \; | awk '{print "| " $9 " | " $5 " |"}' >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Total dist size:** $(du -sh dist | cut -f1)" >> $GITHUB_STEP_SUMMARY

      # ⛔ FUSIONNÉ LE 22/09/2026 — IL Y AVAIT DEUX LISTES, IL N’EN RESTE QU’UNE.
      #
      # DÉCISION DE GAËTAN : « t’analyses, tu merges, et puis tu fais en sorte
      # que ça se fasse normalement. »
      #
      # CE QUI A ÉTÉ RETIRÉ, ET POURQUOI. Quatre étapes nommées à la main
      # vivaient ici — « Requêtes », « Poids », « Topographie », « Contact » —
      # et elles exécutaient EXACTEMENT les mêmes verrous que le passage unique
      # ci-dessous. ⭐ **Deux listes pour la même chose divergent toujours** : un
      # commentaire du 19/09 déclarait encore deux verrous « volontairement
      # dehors » alors que le passage unique les exécutait depuis la veille.
      # *Un fichier qui se contredit fait douter de tout ce qu’il contient.*
      #
      # CE QUI EST GARDÉ, ET CE QUI LE REMPLACE. On garde la liste ÉNUMÉRÉE
      # (`auditer-tout.mjs` découvre les verrous en listant `scripts/`) : un
      # verrou déposé dans le dossier est pris en compte SANS toucher à ce
      # fichier, alors qu’une liste écrite à la main devient fausse en silence.
      # ⚠️ MAIS on ne perd pas ce que les étapes nommées apportaient : dans
      # l’onglet Actions, un échec se lisait comme une ÉTAPE ROUGE portant le
      # nom du verrou fautif. `auditer-tout.mjs` écrit donc désormais un
      # **tableau des verrous** dans `GITHUB_STEP_SUMMARY` (durée, code de
      # sortie, état) — visible sur la page du passage, à côté du journal.
      # *Une fusion qui fait perdre une information n’est pas une fusion,*
      # *c’est une suppression.*
      #
      # ⭐ ET LA RÈGLE SUR LES DEUX VERROUS QUI DÉSIGNENT SANS CONDAMNER :
      #   · `verifier-a11y-rendu.mjs` pilote un Chrome sans interface ;
      #   · `verifier-css-sans-balisage.mjs` désigne des classes mortes sans les
      #     supprimer — il demande un arbitrage, il ne le prend pas.
      # Ils TOURNENT (ils mesurent, et leur verdict est publié), et ils sortent
      # en 0 par défaut : **ils désignent au lieu de condamner.** Le jour où
      # l’un doit bloquer un déploiement, son interrupteur est NOMMÉ et visible :
      #     A11Y_BLOQUANT=1  ·  CSS_BLOQUANT=1  ·  MIROIRS_BLOQUANT=1
      # ⛔ Un verrou qui échoue sur une décision qu’il n’a pas le droit de prendre
      # bloque la CI pour rien. *Il rapporte ; on décide.*
      # ⭐ LE POINT DE PASSAGE UNIQUE — ajoute le 22/09/2026.
      # Il decouvre les verrous par ENUMERATION du dossier, pas par une liste ecrite a
      # la main : un verrou depose dans scripts/ est pris en compte sans toucher ce
      # fichier. Les quatre etapes ci-dessus nomment les pannes ; celle-ci garantit
      # qu'AUCUN verrou n'est oublie.
      # ⚠️ Mesure du 22/09 : trois verrous existaient et ne tournaient NULLE PART
      #    (presence, a11y-rendu, css-sans-balisage). *Un verrou ecrit mais jamais
      #    lance n'est pas un verrou : c'est un fichier.*
      - name: 🧭 Tous les verrous, un seul passage
        run: node scripts/auditer-tout.mjs

      - name: 📤 Upload build artifact
        uses: actions/upload-artifact@v7
        with:
          name: dist
          path: dist/
          retention-days: 1
          # ⚠ INDISPENSABLE : par défaut, upload-artifact écarte les fichiers
          # cachés. Vérifié le 10/09/2026 en téléchargeant l'artefact : il ne
          # contenait AUCUN fichier commençant par un point, donc `public/.htaccess`
          # n'arrivait jamais jusqu'au job de déploiement — les règles Apache du
          # serveur étaient restées figées à leur version d'origine.
          include-hidden-files: true

  # ═══════════════════════════════════════════════════════════════════════════
  # JOB 3: Lighthouse Audit (partout — la condition « PR only » est retirée)
  # ═══════════════════════════════════════════════════════════════════════════
  lighthouse:
    name: 🔦 Lighthouse Audit
    runs-on: ubuntu-latest
    needs: build

    # ═══════════════════════════════════════════════════════════════════════════
    # CORRIGÉ LE 13/09/2026 — CE JOB NE VERROUILLAIT RIEN. TROIS DÉFAUTS.
    #
    # ① IL NE TOURNAIT QUE SUR LES PULL REQUESTS (`if: github.event_name ==
    #    'pull_request'`). Or ce dépôt déploie **au push sur `main`** : l'audit ne
    #    tournait donc JAMAIS sur le chemin qui met le site en ligne, seulement sur
    #    des PR qui n'arrivent pas quand on pousse directement sur main.
    #    → la condition est retirée : l'audit tourne partout.
    #
    # ② `temporaryPublicStorage: true` TÉLÉVERSAIT LE RAPPORT SUR UN STOCKAGE
    #    PUBLIC TIERS. Deux problèmes : la doctrine du studio est « aucun envoi vers
    #    un service externe », et un rapport Lighthouse contient des extraits de la
    #    page. **Ce paramètre annulait aussi le correctif de `.lighthouserc.json`**,
    #    qui déclare `upload: filesystem` — ici, c'est le workflow qui gagne.
    #    → passé à `false`. `uploadArtifacts` reste : l'artefact est privé au dépôt.
    #
    # ③ `continue-on-error: true` — **LE DÉFAUT QUI ANNULE TOUT LE RESTE.** Même
    #    quand l'audit échoue, le job réussit. Un verrou dont l'échec est ignoré
    #    n'est pas un verrou : c'est un tampon — la formule est du studio, et elle
    #    s'applique à lui-même.
    #    → retiré. L'audit peut désormais ÉCHOUER, et ça se verra.
    #
    # ⚠️ CE JOB NE BLOQUE PAS LE DÉPLOIEMENT, ET C'EST VOULU. `deploy` a
    #    `needs: build`, pas `needs: lighthouse` — les deux tournent en parallèle.
    #    Sur ce dépôt, *pousser EST mettre en ligne*, et cette décision appartient à
    #    Gaëtan, pas à un audit automatique. Le rôle de ce job est de **dire la
    #    vérité en rouge**, pas de retenir la publication.
    # ═══════════════════════════════════════════════════════════════════════════

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v7

      - name: 📥 Download build
        uses: actions/download-artifact@v8
        with:
          name: dist
          path: dist/

      - name: 🔦 Run Lighthouse
        # ⚠ TROIS RÉSERVES MESURÉES LE 13/09/2026, À NE PAS OUBLIER.
        #
        # ① L'INSTRUMENT N'EST PAS ÉPINGLÉ, ET CE N'EST PAS CELUI DES MESURES DE
        #    RÉFÉRENCE. `treosh/lighthouse-ci-action@v11` embarque son propre
        #    Lighthouse : exécuté sur ce poste, `@lhci/cli@0.14.0` a mesuré avec
        #    **Lighthouse 12.1.0**, alors que les mesures consignées dans
        #    `docs/mesurer-la-performance.md` et `modeles/mesure-lighthouse-production-
        #    2026-09-10.md` sont faites avec **13.4.1**. Une version majeure d'écart,
        #    ce n'est pas le même barème : comparer un score de 13.x à un seuil jugé
        #    par 12.x, c'est comparer deux instruments. **À trancher par Gaëtan.**
        #
        # ② `numberOfRuns: 3` (dans .lighthouserc.json) — la doctrine du studio est
        #    « trois tirs, médiane, jamais le meilleur ni le pire ». Le fichier disait
        #    `1` : un seul tir, donc un verdict qui peut basculer sur un aléa de
        #    machine. Corrigé.
        #
        # ③ SUR CE POSTE, LE TIR AVORTE — ET PAS POUR UNE RAISON DE SITE. Le
        #    collecteur `RootCauses` lève « Cannot read properties of undefined
        #    (reading 'frame_sequence') », ce qui fait tomber six audits
        #    (dont `render-blocking-resources`), puis la sortie est en erreur sur un
        #    `EPERM` de nettoyage de profil temporaire Windows. **Le code de sortie 1
        #    ne dit donc rien du site.** C'est un artefact de mesure, de la famille
        #    exacte du `--disable-gpu` déjà payé — et c'est pourquoi ce verrou ne
        #    peut pas être jugé depuis ce poste.
        uses: treosh/lighthouse-ci-action@v11
        with:
          configPath: './.lighthouserc.json'
          uploadArtifacts: true
          temporaryPublicStorage: false

  # ═══════════════════════════════════════════════════════════════════════════
  # JOB 4: Deploy to Production
  # ═══════════════════════════════════════════════════════════════════════════
  deploy:
    name: 🚀 Deploy Production
    runs-on: ubuntu-latest
    needs: build
    # ⭐ CORRIGÉ LE 26/09/2026 : `workflow_dispatch` AJOUTÉ À LA CONDITION.
    # ⛔ LA PANNE, MESURÉE : le run 36208577476, lancé à la main depuis l'onglet
    #    Actions, a rendu « 🚀 Deploy Production · skipped · in 0s ». Le site en ligne
    #    servait donc toujours l'ancienne version — 37 926 octets, identiques avant et
    #    après le run, et aucune erreur nulle part.
    # ⚠️ ET LE PIRE : ce même fichier DÉCRIVAIT DÉJÀ cette panne, quarante lignes plus
    #    bas — « 🚀 Deploy Production : skipped ← le site n'a PAS été mis à jour ».
    #    *Une panne décrite dans un commentaire et non corrigée reste une panne : elle
    #    instruit celui qui la lit et laisse passer celui qui ne la lit pas.*
    # ⇒ Le déclenchement automatique sur push RESTE RETIRÉ — c'est la décision du 26/09,
    #   et elle est bonne : il réécrirait la racine à chaque push. Mais un déclenchement
    #   MANUEL doit pouvoir déployer, sinon l'onglet Actions propose un bouton qui ne
    #   fait rien. *Un bouton qui ne fait rien est pire qu'un bouton absent.*
    if: github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
    environment: production
    
    steps:
      - name: 📥 Download build
        uses: actions/download-artifact@v8
        with:
          name: dist
          path: dist/

      - name: 🚀 Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./dist/
          server-dir: /public_html/
          # ⛔⛔ DEUX EXCLUSIONS, ET ELLES RÉPARENT UNE PANNE MESURÉE LE 26/09/2026.
          #
          # ⚠️ CE QUI SE PASSAIT. `cinematique` déploie `./cinematique/` vers
          #    `/public_html/` — **la racine du domaine**. Et cette étape-ci déployait
          #    `./dist/`, dont `index.html` **est la vitrine ArkAdiA**, vers **la même
          #    racine**. ⇒ **Deux pipelines écrivaient au même endroit, et le dernier
          #    passé gagnait.**
          #
          #    Le commentaire de `deploy-cinematique.yml` l'avait écrit noir sur blanc :
          #      « Le pipeline du site précédent se déclenchait à chaque push sur `main`
          #        et il aurait repris l'accueil au premier push suivant, en silence. »
          #    ⭐ *La panne était NOMMÉE dans le code. Il suffisait de le lire.*
          #
          # ⭐ CE QUI S'EST PASSÉ LE 26/09, dans l'ordre : le site cinématique a été
          #    redéployé, il a repris la racine… puis ce pipeline-ci a terminé derrière
          #    et **la vitrine ArkAdiA a repris la racine**. Mesure : 185 337 octets,
          #    titre « ArkAdiA », au lieu de « GL Digital Lab — Vos outils. ».
          #
          # ⇒ TROIS CHOSES NE SONT PAS DÉPLOYÉES ICI, ET CHACUNE A SA RAISON :
          #     · `/index.html`   — la racine appartient au site cinématique.
          #                         ⭐ La vitrine ArkAdiA a SON adresse : arkadia.gldigitallab.fr,
          #                           servie par `deploy-arkadia-vitrine.yml`. *Un objet à sa
          #                           place n'a pas besoin d'en prendre une autre.*
          #     · `/cinematique/` — ce dossier sert la REDIRECTION de l'ancienne adresse
          #                         vers la racine (déployée par `deploy-cinematique.yml`).
          #                         ⛔ `dist/` en porte une copie qui l'écrasait : `/cinematique/`
          #                           servait 185 337 octets de vitrine au lieu de rediriger.
          #
          # ⚠️ TOUT LE RESTE PASSE : `/dossier`, `/demos/`, `/univers/`, `/plan/`,
          #    `/components`, `/le-pont/`, `/monde/`, `/TARDIS/`… — les 48 pages du
          #    studio restent à leurs adresses. **On n'enlève pas le site : on lui retire
          #    une porte qu'il n'aurait pas dû ouvrir.**
          exclude: |
            /index.html
            /cinematique/**
          dangerous-clean-slate: false

      # ⚠ `.htaccess` : DEUX barrières l'empêchaient d'arriver sur le serveur
      # (constaté le 10/09/2026, journal du déploiement et artefact à l'appui) :
      #   1. `upload-artifact` écartait les fichiers cachés (corrigé ci-dessus) ;
      #   2. l'action FTP n'en parle jamais dans son plan d'envoi (164 fichiers
      #      listés, zéro `.htaccess`) — elle aussi ignore les fichiers cachés.
      # Conséquence mesurée en production : `/arcade` répondait encore 200 avec la
      # page d'accueil alors que le nouveau .htaccess prévoyait un vrai 404, et
      # /assets/ listait toujours ses fichiers. On l'envoie donc explicitement,
      # et l'étape ÉCHOUE si l'envoi échoue : un routage non appliqué en silence
      # est exactement le piège qu'on vient de payer.
      - name: 📄 Envoyer .htaccess (fichier caché, ignoré par les deux étapes)
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
        run: |
          set -euo pipefail
          echo "envoi de dist/.htaccess ($(wc -c < dist/.htaccess) octets)"
          curl --fail --silent --show-error --ftp-create-dirs \
            --user "$FTP_USERNAME:$FTP_PASSWORD" \
            --upload-file dist/.htaccess \
            "ftp://$FTP_SERVER/public_html/.htaccess"
          echo "✅ .htaccess envoyé"

      - name: ✅ Deployment Summary
        run: |
          echo "## 🚀 Deployment Successful!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "🌐 **Site:** https://gldigitallab.fr" >> $GITHUB_STEP_SUMMARY
          echo "📅 **Date:** $(date)" >> $GITHUB_STEP_SUMMARY
          echo "🔖 **Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY

  # ═══════════════════════════════════════════════════════════════════════════
  # JOB 5: Notify Discord
  # ═══════════════════════════════════════════════════════════════════════════
  # ⛔ CORRIGÉ LE 19/09/2026 — L'ALERTE EXISTAIT, ELLE A TOURNÉ, ET ELLE A DIT
  #    LE CONTRAIRE DE CE QUI S'ÉTAIT PASSÉ.
  #
  #    Mesuré sur le run du 18/09 à 20 h 37 (celui du commit « ecole + Arche ») :
  #
  #        🔍 Lint & Quality    : success
  #        📦 Build & Test      : FAILURE     ← le build a échoué
  #        🔦 Lighthouse Audit  : skipped
  #        🚀 Deploy Production : skipped     ← le site n'a PAS été mis à jour
  #        💬 Notify            : success     ← et l'alerte a été envoyée
  #
  #    Le webhook existe (`DISCORD_WEBHOOK`, posé le 07/01/2026), la notification
  #    est partie, et elle disait `status: skipped`. **Le site est resté figé
  #    quatre jours, et le message reçu se lisait « rien ne s'est passé ».**
  #
  #    La cause est en une ligne : `needs: deploy`. Ce job ne regardait QUE le
  #    déploiement — or le déploiement n'a jamais tourné, parce que c'est le
  #    BUILD qui avait échoué. Un surveillant qui ne regarde qu'un maillon ne
  #    voit pas la chaîne s'arrêter au maillon d'avant.
  #
  #    Le correctif : ce job regarde les quatre, et il les NOMME.
  #
  #    ⚠️ `lighthouse` N'EST PAS DANS LE VERDICT, ET C'EST VOULU — voir le
  #    commentaire du job Lighthouse (l.226-230) : *« ce job ne bloque pas le
  #    déploiement, et c'est voulu. Sur ce dépôt, pousser EST mettre en ligne, et
  #    cette décision appartient à Gaëtan, pas à un audit automatique. »* Son
  #    résultat est donc **affiché**, jamais **compté** dans le statut.
  # ═══════════════════════════════════════════════════════════════════════════
  notify:
    name: 💬 Notify
    runs-on: ubuntu-latest
    needs: [lint, build, lighthouse, deploy]
    if: always() && github.ref == 'refs/heads/main'

    steps:
      - name: 💬 Discord Notification
        uses: sarisia/actions-status-discord@v1
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK }}
          # Le verdict porte sur la CHAÎNE QUI MET LE SITE EN LIGNE : lint → build
          # → deploy. Si l'un des trois n'est pas « success », l'alerte est rouge.
          # `skipped` n'est PAS un succès : un déploiement sauté veut dire que le
          # site n'a pas été mis à jour.
          status: ${{ (needs.lint.result == 'success' && needs.build.result == 'success' && needs.deploy.result == 'success') && 'success' || 'failure' }}
          title: ${{ (needs.lint.result == 'success' && needs.build.result == 'success' && needs.deploy.result == 'success') && '✅ Site déployé' || '⛔ ÉCHEC — le site N''A PAS été mis à jour' }}
          description: |
            **Commit :** ${{ github.event.head_commit.message }}
            **Auteur :** ${{ github.actor }}

            **Chaîne de mise en ligne**
            · Lint : ${{ needs.lint.result }}
            · Build : ${{ needs.build.result }}
            · Déploiement : ${{ needs.deploy.result }}

            **Audit (informatif — ne bloque pas la publication)**
            · Lighthouse : ${{ needs.lighthouse.result }}
          url: "https://gldigitallab.fr"
          color: ${{ (needs.lint.result == 'success' && needs.build.result == 'success' && needs.deploy.result == 'success') && '0x10b981' || '0xef4444' }}
        continue-on-error: true

