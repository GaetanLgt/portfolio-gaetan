<template>\n  <nav v-if=\"breadcrumbs.length > 1\" class=\"breadcrumbs\" aria-label=\"Fil d'Ariane\">\n    <ol class=\"breadcrumbs__list\">\n      <li \n        v-for=\"(crumb, index) in breadcrumbs\" \n        :key=\"crumb.path\"\n        class=\"breadcrumbs__item\"\n      >\n        <router-link \n          v-if=\"!crumb.current\"\n          :to=\"crumb.path\"\n          class=\"breadcrumbs__link\"\n          :aria-label=\"`Aller à ${crumb.name}`\"\n        >\n          {{ crumb.name }}\n        </router-link>\n        \n        <span \n          v-else \n          class=\"breadcrumbs__current\"\n          aria-current=\"page\"\n        >\n          {{ crumb.name }}\n        </span>\n        \n        <svg \n          v-if=\"index < breadcrumbs.length - 1\"\n          class=\"breadcrumbs__separator\" \n          width=\"12\" \n          height=\"12\" \n          viewBox=\"0 0 24 24\" \n          fill=\"none\" \n          stroke=\"currentColor\" \n          stroke-width=\"2\"\n          aria-hidden=\"true\"\n        >\n          <polyline points=\"9 18 15 12 9 6\"/>\n        </svg>\n      </li>\n    </ol>\n    \n    <!-- JSON-LD pour les moteurs de recherche -->\n    <script type=\"application/ld+json\">\n      {{\n        {\n          \"@context\": \"https://schema.org\",\n          \"@type\": \"BreadcrumbList\",\n          \"itemListElement\": breadcrumbsJsonLd\n        }\n      }}\n    </script>\n  </nav>\n</template>\n\n<script setup>\nimport { computed } from 'vue';\nimport { useSEOOptimization } from '@/composables/useSEOOptimization';\n\nconst { generateBreadcrumbs } = useSEOOptimization();\n\nconst breadcrumbs = computed(() => generateBreadcrumbs());\n\n// JSON-LD pour Schema.org\nconst breadcrumbsJsonLd = computed(() => {\n  return breadcrumbs.value.map((crumb, index) => ({\n    \"@type\": \"ListItem\",\n    \"position\": index + 1,\n    \"name\": crumb.name,\n    \"item\": `https://gldigitallab.fr${crumb.path}`\n  }));\n});\n</script>\n\n<style scoped>\n.breadcrumbs {\n  padding: var(--space-sm) 0;\n  margin-bottom: var(--space-md);\n}\n\n.breadcrumbs__list {\n  display: flex;\n  align-items: center;\n  flex-wrap: wrap;\n  gap: 0.5rem;\n  list-style: none;\n  margin: 0;\n  padding: 0;\n  font-size: 0.875rem;\n}\n\n.breadcrumbs__item {\n  display: flex;\n  align-items: center;\n  gap: 0.5rem;\n}\n\n.breadcrumbs__link {\n  color: var(--text-muted);\n  text-decoration: none;\n  transition: var(--transition-fast);\n  padding: 0.25rem 0.5rem;\n  border-radius: 0.25rem;\n  font-weight: 400;\n}\n\n.breadcrumbs__link:hover {\n  color: var(--primary);\n  background: rgba(16, 185, 129, 0.1);\n}\n\n.breadcrumbs__link:focus-visible {\n  outline: 2px solid var(--primary);\n  outline-offset: 2px;\n}\n\n.breadcrumbs__current {\n  color: var(--text-main);\n  font-weight: 500;\n  padding: 0.25rem 0.5rem;\n}\n\n.breadcrumbs__separator {\n  color: var(--text-dark);\n  flex-shrink: 0;\n}\n\n/* Mobile responsive */\n@media (max-width: 640px) {\n  .breadcrumbs {\n    padding: var(--space-xs) 0;\n  }\n  \n  .breadcrumbs__list {\n    font-size: 0.8rem;\n    gap: 0.25rem;\n  }\n  \n  .breadcrumbs__link,\n  .breadcrumbs__current {\n    padding: 0.125rem 0.25rem;\n  }\n  \n  /* Cache les éléments intermédiaires sur mobile si plus de 3 niveaux */\n  .breadcrumbs__item:nth-child(n+2):nth-last-child(n+3) {\n    display: none;\n  }\n  \n  /* Ajoute \"...\" quand on cache des éléments */\n  .breadcrumbs__item:nth-child(2)::before {\n    content: \"...\";\n    color: var(--text-dark);\n    margin-right: 0.5rem;\n  }\n}\n\n/* High contrast mode */\n@media (prefers-contrast: high) {\n  .breadcrumbs__link {\n    border: 1px solid var(--border);\n  }\n  \n  .breadcrumbs__current {\n    background: var(--surface);\n    border: 1px solid var(--primary);\n  }\n}\n</style>", "oldText": "<template>\n  <nav v-if=\"breadcrumbs.length > 1\" class=\"breadcrumbs\" aria-label=\"Fil d'Ariane\">\n    <ol class=\"breadcrumbs__list\">\n      <li \n        v-for=\"(crumb, index) in breadcrumbs\" \n        :key=\"crumb.path\"\n        class=\"breadcrumbs__item\"\n      >\n        <router-link \n          v-if=\"!crumb.current\"\n          :to=\"crumb.path\"\n          class=\"breadcrumbs__link\"\n          :aria-label=\"`Aller à ${crumb.name}`\"\n        >\n          {{ crumb.name }}\n        </router-link>\n        \n        <span \n          v-else \n          class=\"breadcrumbs__current\"\n          aria-current=\"page\"\n        >\n          {{ crumb.name }}\n        </span>\n        \n        <svg \n          v-if=\"index < breadcrumbs.length - 1\"\n          class=\"breadcrumbs__separator\" \n          width=\"12\" \n          height=\"12\" \n          viewBox=\"0 0 24 24\" \n          fill=\"none\" \n          stroke=\"currentColor\" \n          stroke-width=\"2\"\n          aria-hidden=\"true\"\n        >\n          <polyline points=\"9 18 15 12 9 6\"/>\n        </svg>\n      </li>\n    </ol>\n    \n    <!-- JSON-LD pour les moteurs de recherche -->\n    <script type=\"application/ld+json\">\n      {{\n        {\n          \"@context\": \"https://schema.org\",\n          \"@type\": \"BreadcrumbList\",\n          \"itemListElement\": breadcrumbsJsonLd\n        }\n      }}\n    </script>\n  </nav>\n</template>\n\n<script setup>\nimport { computed } from 'vue';\nimport { useSEOOptimization } from '@/composables/useSEOOptimization';\n\nconst { generateBreadcrumbs } = useSEOOptimization();\n\nconst breadcrumbs = computed(() => generateBreadcrumbs());\n\n// JSON-LD pour Schema.org\nconst breadcrumbsJsonLd = computed(() => {\n  return breadcrumbs.value.map((crumb, index) => ({\n    \"@type\": \"ListItem\",\n    \"position\": index + 1,\n    \"name\": crumb.name,\n    \"item\": `https://gldigitallab.fr${crumb.path}`\n  }));\n});\n</script>\n\n<style scoped>\n.breadcrumbs {\n  padding: var(--space-sm) 0;\n  margin-bottom: var(--space-md);\n}\n\n.breadcrumbs__list {\n  display: flex;\n  align-items: center;\n  flex-wrap: wrap;\n  gap: 0.5rem;\n  list-style: none;\n  margin: 0;\n  padding: 0;\n  font-size: 0.875rem;\n}\n\n.breadcrumbs__item {\n  display: flex;\n  align-items: center;\n  gap: 0.5rem;\n}\n\n.breadcrumbs__link {\n  color: var(--text-muted);\n  text-decoration: none;\n  transition: var(--transition-fast);\n  padding: 0.25rem 0.5rem;\n  border-radius: 0.25rem;\n  font-weight: 400;\n}\n\n.breadcrumbs__link:hover {\n  color: var(--primary);\n  background: rgba(16, 185, 129, 0.1);\n}\n\n.breadcrumbs__link:focus-visible {\n  outline: 2px solid var(--primary);\n  outline-offset: 2px;\n}\n\n.breadcrumbs__current {\n  color: var(--text-main);\n  font-weight: 500;\n  padding: 0.25rem 0.5rem;\n}\n\n.breadcrumbs__separator {\n  color: var(--text-dark);\n  flex-shrink: 0;\n}\n\n/* Mobile responsive */\n@media (max-width: 640px) {\n  .breadcrumbs {\n    padding: var(--space-xs) 0;\n  }\n  \n  .breadcrumbs__list {\n    font-size: 0.8rem;\n    gap: 0.25rem;\n  }\n  \n  .breadcrumbs__link,\n  .breadcrumbs__current {\n    padding: 0.125rem 0.25rem;\n  }\n  \n  /* Cache les éléments intermédiaires sur mobile si plus de 3 niveaux */\n  .breadcrumbs__item:nth-child(n+2):nth-last-child(n+3) {\n    display: none;\n  }\n  \n  /* Ajoute \"...\" quand on cache des éléments */\n  .breadcrumbs__item:nth-child(2)::before {\n    content: \"...\";\n    color: var(--text-dark);\n    margin-right: 0.5rem;\n  }\n}\n\n/* High contrast mode */\n@media (prefers-contrast: high) {\n  .breadcrumbs__link {\n    border: 1px solid var(--border);\n  }\n  \n  .breadcrumbs__current {\n    background: var(--surface);\n    border: 1px solid var(--primary);\n  }\n}\n</style>"}]